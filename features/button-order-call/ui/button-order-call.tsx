import { Button } from "@/shared/ui/button"
import styles from './button-order-call.module.scss'
export const ButtonOrderCall = () => {
    return <Button variant="ghost" className={styles.button}>Заказать звонок</Button>
}